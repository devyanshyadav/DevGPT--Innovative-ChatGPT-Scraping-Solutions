commands:
  01_install_chrome:
    command: |
      curl https://dl.google.com/linux/direct/google-chrome-stable_current_x86_64.rpm -o /tmp/chrome.rpm
      yum install -y /tmp/chrome.rpm
      rm /tmp/chrome.rpm